# v0.2.0a1 External Release Decision Placeholder

**Kind:** Placeholder record for the external, human-owned release
decision that gates the prospective `v0.2.0a1` Mini-SGLang Ascend
technical-preview tag. This file is documentation-only. It creates
no tag, no GitHub Release, no `CHANGELOG.md` entry, no version
bump, no history rewrite, no force-push, no credential printing,
and no runtime, script, or test change. The purpose of this
document is to reserve the slots into which the human release
owner will later record their decision.

---

## 1. Current RC status

* RC notes ready at `80c750f` (merge of Gate 5.10 into
  `ascend-port`).
* Release-candidate notes document:
  [`v0.2.0a1_release_candidate_notes.md`](./v0.2.0a1_release_candidate_notes.md).
* Release cut is still **BLOCKED**. No `v0.2.0a1` tag has been
  created. No GitHub Release for `v0.2.0a1` has been drafted or
  published. `CHANGELOG.md` has no `v0.2.0a1` entry.

## 2. External decision required

The following items are strictly external to any documentation
gate in this repository and must be resolved by the human release
owner before a `v0.2.0a1` tag may be cut:

* **Credential rotation:** UNKNOWN / EXTERNAL.
  Whether any credentials that were once printed in tracked Git
  history — before the Gate 4.13a / 4.13b HEAD redaction sweep —
  have been rotated at their source (SSH host password, container
  access token, private-network endpoint, etc.). This repository
  has no authoritative view of the source systems and cannot
  confirm rotation state.
* **Git history hygiene:** NOT CLOSED.
  The Gate 4.13a / 4.13b redaction sweep applied to the tracked
  HEAD only. Historical Git blobs from before those sweeps have
  not been enumerated, quoted, verified, or removed by any Gate.
* **Human release owner must decide before tag creation.**
  Neither this document nor any prior Gate authorises the release
  cut. The decision is reserved for the human release owner in
  their own signed record — either on the tag / release body
  itself, or in a follow-up gate that explicitly consumes this
  placeholder.

## 3. Decision slots

The release owner records their position by checking exactly one
of the following slots in a **later, separate**, human-owned
commit. All three slots are recorded here as unchecked and must
remain unchecked in this Gate 5.11 document.

```
[ ] release owner acknowledges risk and permits v0.2.0a1 tag
[ ] release owner requires credential rotation first
[ ] release owner requires history rewrite / repo replacement first
```

Gate 5.11 does **not** check any of the slots above. If a future
gate or the release owner checks one, they must do so in their
own commit and their own record, and must explicitly reference
this placeholder file by path.
