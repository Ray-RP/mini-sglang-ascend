# v0.2.0a1 Credential Rotation Checklist

**Kind:** Human-owned credential-rotation checklist for the
prospective `v0.2.0a1` Mini-SGLang Ascend technical-preview tag.
Documentation-only. Creates no tag, no GitHub Release, no
`CHANGELOG.md` entry, no version bump, no history rewrite, no
force-push, no credential printing, and no runtime, script, or
test change. This file reserves the checklist slots the human
release owner must resolve before authorising a `v0.2.0a1` cut.

---

## 1. Status

* `v0.2.0a1` RC notes are ready — see
  [`v0.2.0a1_release_candidate_notes.md`](./v0.2.0a1_release_candidate_notes.md).
* Release cut is **blocked**. No `v0.2.0a1` tag exists. No
  GitHub Release for `v0.2.0a1` has been drafted. `CHANGELOG.md`
  has no `v0.2.0a1` entry.
* Credential-rotation status is **UNKNOWN / EXTERNAL** — see
  [`v0.2.0a1_external_release_decision.md`](./v0.2.0a1_external_release_decision.md).
  This repository has no authoritative view of the source
  systems and cannot verify rotation state on its own.

## 2. Required human checks

The following checks are the human release owner's
responsibility. All slots are recorded here as unchecked and
must remain unchecked in this Gate 5.12 document. The release
owner (or a later, separate, human-owned gate) is the only
party authorised to check them.

```
[ ] old SSH password rotated or disabled
[ ] old host access reviewed
[ ] old container access reviewed
[ ] deploy keys / tokens reviewed, if any
[ ] release owner records final decision in
    v0.2.0a1_external_release_decision.md
```

Gate 5.12 does **not** check any of the slots above. Under no
circumstances does this document print, quote, or enumerate any
real historical credential substring, host address, username, or
container identifier.

## 3. Rule

**Do not cut `v0.2.0a1` until the checklist above is resolved by
the human release owner.**

Neither this document nor any prior Gate authorises the release
cut. When the release owner (or the release-cut gate) resolves
the checklist, they must do so in their own commit and their own
signed record, and must explicitly reference this checklist file
and
[`v0.2.0a1_external_release_decision.md`](./v0.2.0a1_external_release_decision.md)
by path.
